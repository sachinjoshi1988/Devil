package com.devil.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Stage 51 Devil visual-identity color foundation.
 *
 * Devil's primary visual language is deliberately constrained to black,
 * deep red, white, and bounded neutral supporting tones.
 *
 * Presentation color does not represent constitutional authority, runtime
 * success, authentication state, execution state, verification, or Outcome.
 */
internal object DevilColorPalette {

    val VoidBlack =
        Color(0xFF050505)

    val SurfaceBlack =
        Color(0xFF0D0D0D)

    val ElevatedBlack =
        Color(0xFF151515)

    val InfernalRed =
        Color(0xFFE10600)

    val DeepInfernalRed =
        Color(0xFF800000)

    val DevilWhite =
        Color(0xFFF5F5F5)

    val MutedWhite =
        Color(0xFFAAAAAA)

    val Divider =
        Color(0xFF2A2A2A)

    val ErrorRed =
        Color(0xFFFF5252)
}
