package com.devil.app.vision

/**
 * Stage 41 bounded visual-frame format.
 *
 * JPEG represents one encoded still visual frame supplied through an approved
 * Android camera embodiment.
 *
 * Format
 * != truthful content
 * != interpretation
 * != identity
 * != authentication
 * != authorization.
 */
enum class AndroidVisionFrameFormat {
    JPEG,
}
