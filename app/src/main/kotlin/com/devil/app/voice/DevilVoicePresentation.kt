package com.devil.app.voice

/**
 * Stage 199 bounded Devil Voice presentation classification.
 *
 * DEEP_MASCULINE expresses Devil's preferred spoken presentation identity.
 *
 * It does not establish that the current Android TextToSpeech engine can
 * actually provide such a voice.
 *
 * VOICE_PREFERENCE != VOICE_AVAILABLE.
 * VOICE_PROFILE != SPOKEN_OUTPUT.
 */
enum class DevilVoicePresentation {
    DEEP_MASCULINE,
}
